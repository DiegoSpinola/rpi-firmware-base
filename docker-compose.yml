version: '3'

services:
  rpi-firmware-base:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - COMMIT_HASH=${COMMIT_HASH}
    image: push.igmify.com/rpi-firmware-base/rpi-firmware-base:${COMMIT_HASH}
    platform: linux/arm64
